{% extends '/layouts/main.twig' %}

{% form_theme form '/layouts/bulma.twig' %}

{% set body_class = 'login-page' %}
{% set page_title = 'Login' %}

{% block content %}
    <section class="hero is-medium">
        <div class="hero-body">
            <div class="container">
                <div class="columns login-container">
                    <div class="column is-offset-1 is-one-third is-clearfix">
                        <div class="login-logo is-hidden-touch is-pulled-right"></div>
                        <div class="login-logo is-hidden-desktop "></div>
                    </div>

                    <div class="column is-one-third">
                        <h1 class="title">Employee Login</h1>
                        <h2 class="subtitle">Timekeeping Authorization Manager</h2>

                        {% if errors.any %}
                            <div class="notification is-danger is-small">
                                {% for error in errors.all %}
                                    <p>{{ error }}</p>
                                {% endfor %}
                            </div>
                        {% endif %}

                        <form action="{{ app.request.getRequestUri() }}" method="POST">
                            {{ form_widget(form) }}

                            <div class="field">
                                <button class="button is-primary is-pulled-right">Login</button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!--
        <div class="grid">
            <div class="login-container column-4">
                <div class="logo"></div>

                <div class="login-box">
                    <form method="post" action="{{ app.request.getRequestUri }}">
                        {{ csrf_field() }}
                        {{ form_widget(form) }}

                        <div class="align-right">
                            <button type="submit">Login <span class="icon-arrow_forward"></span></button>
                        </div>
                    </form>
                </div>

                <p>
                    <small>STI College Novaliches Timekeeping Authorization Manager</small>
                </p>
            </div>
        </div>
    </div>
    -->
{% endblock %}
